The 2 files are only examples. The real ones are deployed in the server. 
In the local development environment these config files are used: /sharks-config/src/main/resources

Erik second test change.
  